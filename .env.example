# Root .env is not used directly by the services.
# - Backend config:  see backend/.env.example  (copy to backend/.env)
# - Frontend config: NEXT_PUBLIC_API_URL (see frontend)
#
# This file is kept only as a pointer to the authoritative examples above.
