export type GuidedPlacement = "top" | "bottom" | "left" | "right" | "center";

/** block = orientation tour (no click-through). guide = tutorial (hole clickable). */
export type SpotlightInteractionMode = "block" | "guide";
